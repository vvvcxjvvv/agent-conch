# 2026-06-26 工作日志（续）

## 项目实现：按 v0.3 方案落地代码 + 技术点文档

### 1. 项目骨架搭建
- 创建完整目录结构（conch/core + conch/domains/9域 + conch/runtime + profiles + benchmarks + docs/technical-points + tests）
- pyproject.toml（依赖 pydantic/pyyaml/opentelemetry，可选 docker/chromadb/playwright/litellm）
- README.md、AGENTS.md（dogfooding）
- 所有包 __init__.py

### 2. 核心抽象层 conch/core/（7 个文件，亲自实现保证一致性）
- extension.py：ExtensionPoint 契约 + 9 域 Protocol 接口定义 + 依赖倒置
- registry.py：装饰器注册 + depends_on 拓扑加载 + 生命周期钩子 + query 运行时发现 + 版本共存
- hooks.py：Hook 总线 + 三大约束（职责隔离/优先级/中断白名单）+ HookResult/HookInterrupted
- middleware.py：Pipeline 中间件链（数据流处理，禁止中断）
- profile.py：Profile + extends 继承 + Pydantic 校验 + 环境变量覆盖
- loop.py：AgentLoop + State + CostGuard 分级降级（L1压缩→L2切模型→L4终止）+ streaming
- experiment.py：实验框架 + 对比表 + 消融实验 + SWE-bench/swe-mini 对接

### 3. 技术点文档 docs/technical-points/（13 篇 + 索引）
- 01 扩展点契约、02 注册中心、03 Hook与中间件、04 Profile与实验框架
- 05 Agent Loop、06 上下文管理、07 工具系统与MCP、08 记忆五分法
- 09 多Agent协作、10 可观测性与自观测、11 沙箱与安全治理、12 成本守卫、13 Skill系统
- README.md 索引（含阅读建议）

### 4. 域实现 + 运行时层（agent 协助 + lead 补齐）
- 9 域默认实现全部完成（agent 写代码 + lead 补运行时层/CLI/profile/benchmark）
- 运行时层：MockProvider + DockerSandbox（加固基线）+ MemoryStore
- 示例 Profile：coding-agent-v1 + coding-agent-v2-subagents（extends 继承）
- benchmark：swe-mini/001.json + 002.json
- CLI 入口：conch/__main__.py

### 5. 验证结果
- 11 个单元测试全部通过（test_core.py）
- CLI plugins 命令：9 域全部展示
- CLI run 命令：完整 Agent Loop 跑通（MockProvider，50步）
- 端到端 demo（demo_e2e.py）：ScriptedProvider 模拟"读文件→修复→写文件"全链路，6步完成，文件正确修复
- 实验框架：swe-mini 3任务×1Profile，输出 Markdown 对比表

### 6. 关键改进（继续实现阶段）
- profile.py 改为双模式：有 pydantic+yaml 用完整校验，无则用纯 Python dataclass + 内置简易YAML解析器（保证零依赖可跑）
- loop.py 新增 _idle_steps()：连续3步无工具调用自动终止（避免空转）
- 新增 ScriptedProvider（runtime/model/scripted.py）：按预设脚本返回响应，用于端到端测试
- 新增 tests/test_core.py（11个测试）+ tests/demo_e2e.py（端到端demo）

### 7. 文档更新
- README.md 重写 + docs/implementation-status.md

### 8. Web 交互界面
- 后端：conch/web/server.py（FastAPI + WebSocket 实时流 + REST API）
  - REST: /api/health /api/plugins /api/profiles /api/benchmarks
  - WebSocket: /ws/run 实时推送 step/tool/cost 事件
  - Hook 总线注入 WebSocket 推送：pre_step/post_step/pre_tool/post_tool/on_cost_exceeded
- 前端：conch/web/static/index.html（Vue 3 CDN，简洁亮色风格）
  - 左侧对话区：消息流 + 工具调用卡片 + 输入框 + Demo 按钮
  - 右侧轨迹面板：指标卡片(Status/Steps/Tokens/Cost) + Step 列表(可展开详情)
  - WebSocket 实时渲染：task_start/step_start/step_end/tool_result/cost_alert/task_end
  - Profile 选择器自动从 /api/profiles 加载
- 启动：python -m conch.web.server → http://localhost:8420
- 已验证：全部 API 正常 + 前端页面可访问 + Demo 模式可跑
