# 2026-06-27 工作日志

## Web UI 交互体验修复
- 改造 `conch/web/server.py` 的 `_handle_run_task`：从 MockProvider（死文本）改为智能 ScriptedProvider，根据任务内容生成不同工具调用脚本（修复类→read→write，探索类→list→read，对话类→list+文本）
- 前端 `index.html` 加模式提示横幅 + 空状态示例芯片 + useExample 方法
- 修复端口占用问题（PID 24272 占用 8420 端口）

## 实现状态差距分析
- 用户问"是否完全实现了 technical-design 中的功能点"
- 对比 v0.3 技术设计文档与实际代码，结论：**没有完全实现**
- 核心抽象层 7/7 完整实现，运行时层 3/4
- 9 域各有一个最小骨架实现，但整体技术点覆盖率约 40%（25/62）
- 最关键缺口：Skill系统（设计文档专门写了一节但完全没实现）、真实LLM接入、多Agent协作、OTel集成
- 路线图仅完成阶段一 MVP，阶段 1.5/二/三/四均未开始
- 生成可视化状态矩阵图展示 9 域进度

## v2.0 技术方案生成（推翻重做）
- 用户要求推翻之前 agent-conch 实现，参考提供的 HarnessEngineering_实现方案.md（ETCLOVG 七层），结合业界成熟实践，重新设计 harness 应用
- 用户拍板三个决策：①成熟框架底座+可扩展层 ②先用户前台后开发者后台 ③Python(FastAPI)+React/Next.js
- 产出 `docs/technical-design-v2.md`（v2.0 技术方案，约 600 行）
- 核心创新：Hook 桥接层（core/hook_bridge.py），把框架原生事件（LangGraph callbacks）桥接到框架无关的语义 Hook 总线，换编排引擎只需新写桥接 Plugin，已有护栏/审计/成本守卫零改动复用
- 架构：五层（前端Next.js / API网关FastAPI / Harness可扩展层core / 成熟框架底座adapters / 运行时）
- 成熟框架底座：LangGraph编排 + MCP工具 + Mem0记忆 + NeMo/LlamaGuard护栏 + Langfuse可观测 + DeepEval评测 + litellm多模型
- 迁移策略：conch/core/ 8文件 80%保留（3直接复用+4改造复用+1拆分改造），9域骨架推翻为adapters，静态HTML推翻为Next.js
- 分四阶段：MVP用户前台(1-2周) → 生产加固(2-4周) → 开发者后台+实验(2-3周) → 多Agent+治理(2-3周)，每阶段量化退出标准

## v2 MVP 代码实现（推翻 v1，基于 v2 方案重写）
- 用户确认：只做阶段一 MVP / backend+frontend 结构 / 直接删除 v1 / 三栏完整布局 / 本地国内模型 / NeMo+MCP必装
- **Step 0 清理迁移**：删除 conch/domains/ conch/web/ runtime/model/scripted.py tests/；迁移 core/ 8文件+sandbox+5个可复用文件到 backend/conch/
- **Step 1 core 改造**：extension.py 加 guardrail 域(第10域)+GuardrailProvider+GuardrailResult；新建 cost_guard.py(从loop.py拆)；新建 hook_bridge.py(LangGraph callback→语义Hook)；新建 guardrail_pipeline.py(input/output两路)；删除 loop.py
- **Step 2 adapters**：新建6个(litellm_provider/langgraph_react/mcp_provider/nemo_guardrails/langfuse_tracer/agents_md)+迁移改造5个(single_loop重写用litellm/builtin_shell/console_tracer/jit_compaction/notes_file)，共11个全部注册成功
- **Step 3 API层**：FastAPI app + deps.py(build_runtime) + sse.py + routes/{chat SSE流式/session/profile/plugin} + CORS
- **Step 4 前端**：Next.js 14 三栏布局(Sidebar会话列表/Conversation对话+工具卡片+护栏提示/MetricsPanel实时指标) + zustand store + sse-client(fetch+ReadableStream解析) + lib/api.ts
- **Step 5 验证**：8项core单元测试全通过(DOMAINS含guardrail/CostGuard分级/护栏拦截/Hook桥接/adapter注册/0改动扩展)；user-chat-v1.yaml demo Profile；NeMo配置；5预设任务
- **Step 6 文档**：backend/pyproject.toml(v2依赖) + frontend/package.json + AGENTS.md更新(10域+adapters) + docs/implementation-status-v2.md(实现状态记录)
- 测试验证：`test_zero_code_extension` 证明核心0改动接入新插件成立；adapters 11个全部注册到 registry
- v1 完全推翻：根目录 conch/ 和 pyproject.toml 和 profiles/ 已删除，只保留 docs/ benchmarks/ AGENTS.md README.md 作历史资产
